'use client';

export function BingoCard() {
  return (
    <div className="rounded-lg border border-dashed p-6 text-sm text-muted-foreground">
      Bingo UI is not installed in this build. Use chat commands to run the bingo game.
    </div>
  );
}